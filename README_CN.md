# TradeReAct: 增强型多智能体LLM金融交易框架

<div align="center">
  <a href="README.md">English</a> | <a href="README_CN.md">简体中文</a>
</div>

> 🚀 **TradeReAct** 是 [TradingAgents](https://github.com/TauricResearch/TradingAgents) 的改进版本，集成了 MCP（模型上下文协议）、Weaviate 向量数据库支持以及优化的内存管理，提供增强的交易智能。

<div align="center">

🚀 [核心特性](#核心特性) | 🏗️ [架构设计](#架构设计) | ⚡ [安装指南](#安装指南) | 🔧 [配置说明](#配置说明) | 📦 [使用方法](#使用方法) | 🎯 [改进亮点](#相比原版的改进)

</div>

---

## 概述

TradeReAct 是一个多智能体交易框架，模拟真实交易公司的运作机制。通过部署专业的 LLM 驱动的智能体——从基本面分析师、情绪分析专家、技术分析师到交易员和风险管理团队——该平台协同评估市场状况并做出交易决策。

**TradeReAct 的独特之处：**
- 🔌 **MCP 集成**: 通过模型上下文协议连接外部数据源
- 🗄️ **Weaviate 向量数据库**: 高级内存系统，支持混合搜索（BM25 + 向量相似度）
- 🎯 **灵活配置**: 配置驱动的架构，易于定制
- 🌍 **多市场支持**: 通过 MCP 工具增强对中国股市的支持
- ⚡ **性能优化**: 改进的内存管理和路由效率

> TradeReAct 框架仅用于研究目的。交易表现可能因多种因素而异。[本框架不构成财务、投资或交易建议。](https://tauric.ai/disclaimer/)

---

## 核心特性

### 🏗️ 多智能体架构

> 📊 **[查看架构图](#架构设计)** - LangGraph 工作流图与各阶段子图。

我们的框架将复杂的交易任务分解为专业角色：

#### 分析师团队
- **基本面分析师**: 评估公司财务和绩效指标，可选配 MCP 工具获取实时数据
- **情绪分析师**: 使用情绪评分算法分析社交媒体和公众情绪
- **新闻分析师**: 监控全球新闻和宏观经济指标，集成 MCP 获取多样化新闻源
- **技术分析师**: 利用技术指标（MACD、RSI 等）检测交易模式

#### 研究员团队
- 包含看多和看空研究员，批判性评估分析师见解
- 通过结构化辩论平衡潜在收益与固有风险
- 使用向量内存回忆相关历史情况，做出明智讨论

#### 交易员智能体
- 综合分析师和研究员的报告做出明智的交易决策
- 基于全面的市场洞察确定交易时机和规模
- 访问历史交易内存进行模式识别

#### 风险管理和投资组合经理
- 通过评估波动性、流动性和风险因素持续评估投资组合风险
- 向投资组合经理提供评估报告以做最终决策
- 维护风险内存以确保一致的风险评估标准

---

## 架构设计

### 顶层 LangGraph

![TradeReAct LangGraph](graphs/tradereact_graph.png)

### 各阶段子图

![Analyst 子图](graphs/analyst_subgraph.png)

![Researcher 子图](graphs/researcher_subgraph.png)

![Risk 子图](graphs/risk_subgraph.png)

### 🔌 MCP（模型上下文协议）集成

TradeReAct 支持 MCP 服务器，在**现有数据源基础上扩展**智能体能力。MCP 工具与自定义工具（yfinance、Alpha Vantage 等）无缝合并，提供统一的工具接口。

**核心优势**：
- ✅ **增量式扩展，而非替代**: MCP 工具补充现有数据源
- ✅ **统一接口**: 所有工具（自定义 + MCP）实现 BaseTool 接口，无缝集成
- ✅ **配置驱动**: 无需修改代码即可为分析师启用/禁用 MCP
- ✅ **向后兼容**: MCP 不可用时自动回退到自定义工具
- ✅ **多市场支持**: 轻松添加区域特定数据源（如中国 A 股）

**配置示例**：

```json
{
  "mcpServers": {
    "market_analyst": {
      "enabled": true,
      "description": "提供中国股市数据接口",
      "servers": {
        "china-stock-mcp": {
          "url": "https://mcp.api-inference.modelscope.net/2334ad93fd4b44/mcp",
          "transport": "streamable_http"
        }
      }
    }
  }
}
```

**工作原理**：
1. **首先加载自定义工具**: yfinance 获取价格，Alpha Vantage 获取基本面/新闻
2. **添加 MCP 工具**: 从配置的 MCP 服务器获取额外工具
3. **合并工具列表**: 智能体接收所有可用工具的组合列表
4. **智能体选择**: ReAct 智能体为每个任务选择最合适的工具

**可用 MCP 工具（market_analyst 示例 - 30+ 工具）**：
- 历史和实时股票数据（中国 A 股）
- 财务报表（资产负债表、利润表、现金流量表）
- 市场分析（资金流向、情绪、波动性）
- 公司信息和股东数据
- 估值指标和研究报告
- 指数数据和市场统计

> 📊 查看 [架构图](docs/architecture_diagram.md) 了解 MCP 工具加载和合并流程的可视化表示。

### 🗄️ Weaviate 向量数据库

增强的内存系统，具备：
- **混合搜索**: 结合 BM25 关键词搜索和向量相似度（可配置 alpha 参数）
- **5 个专用集合**: 为看多/看空研究员、交易员、分析师子图和风险管理分别设置内存
- **持久化存储**: 支持本地或云端 Weaviate 实例
- **语义理解**: 更好的上下文检索，支持智能体决策

---

## 安装指南

### 前置要求

- Python 3.10+
- Conda 或虚拟环境管理器
- Weaviate（内置模式开箱即用，也可配置本地/云实例）

### 克隆仓库

```bash
git clone https://github.com/yourusername/TradeReAct.git
cd TradeReAct
```

### 创建虚拟环境

```bash
conda create -n tradereact python=3.10
conda activate tradereact
```

### 安装依赖

```bash
pip install -r requirements.txt
```

### 必需的 API 密钥

您需要：
- **OpenAI API**: 用于 LLM 智能体
- **Alpha Vantage API**（可选）: 用于基本面和新闻数据（[免费获取](https://www.alphavantage.co/support/#api-key)）

在项目根目录创建 `.env` 文件：

```bash
cp .env.example .env
# 编辑 .env 填入您的实际 API 密钥
```

示例 `.env`：
```bash
OPENAI_API_KEY=your_openai_api_key_here
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key_here
# 可选：自定义 OpenAI 端点
OPENAI_BASE_URL=https://api.openai.com/v1
```

---

## 配置说明

### 默认配置

编辑 [tradereact/default_config.py](tradereact/default_config.py) 进行自定义：

```python
DEFAULT_CONFIG = {
    # LLM 设置
    "deep_think_llm": "o4-mini",        # 推理模型
    "quick_think_llm": "gpt-4o-mini",   # 快速模型

    # 辩论设置
    "max_debate_rounds": 1,
    "max_risk_discuss_rounds": 1,

    # 数据供应商
    "data_vendors": {
        "core_stock_apis": "yfinance",
        "technical_indicators": "yfinance",
        "fundamental_data": "alpha_vantage",
        "news_data": "alpha_vantage",
    },

    # Weaviate 配置（即将推出）
    "weaviate": {
        "host": "localhost",
        "port": 50051,
        "use_embedded": True,  # 设为 False 使用本地实例
    }
}
```

### MCP 配置

编辑 [mcp_config.json](mcp_config.json) 为特定分析师配置 MCP 服务器：

```json
{
  "mcpServers": {
    "market_analyst": {
      "enabled": true,
      "description": "您的 MCP 服务器描述",
      "servers": {
        "your-mcp-server": {
          "url": "your-mcp-server-url",
          "transport": "streamable_http"
        }
      }
    }
  }
}
```

**测试 MCP 配置**：
```bash
python -m tradereact.agents.utils.mcp_loader
```

---

## 使用方法

### Python API

基础用法：

```python
from tradereact.graph.trading_graph import TradingAgentsGraph
from tradereact.default_config import DEFAULT_CONFIG

# 初始化图
ta = TradingAgentsGraph(debug=True, config=DEFAULT_CONFIG.copy())

# 运行交易分析
_, decision = ta.propagate("NVDA", "2024-05-10")
print(decision)
```

自定义配置：

```python
from tradereact.graph.trading_graph import TradingAgentsGraph
from tradereact.default_config import DEFAULT_CONFIG

# 创建自定义配置
config = DEFAULT_CONFIG.copy()
config["deep_think_llm"] = "gpt-4o"
config["quick_think_llm"] = "gpt-4o-mini"
config["max_debate_rounds"] = 2

# 配置数据供应商
config["data_vendors"]["fundamental_data"] = "alpha_vantage"
config["data_vendors"]["news_data"] = "openai"

# 使用自定义配置初始化
ta = TradingAgentsGraph(debug=True, config=config)

# 运行分析
_, decision = ta.propagate("AAPL", "2024-12-01")
print(decision)
```

### CLI 使用

```bash
python -m cli.main
```

CLI 提供交互式界面以：
- 选择要分析的股票代码
- 选择分析日期
- 配置 LLM 模型
- 设置研究深度
- 查看实时智能体进度

---

## 相比原版的改进

### 🔌 1. MCP 集成

**功能**: 通过标准化协议扩展智能体能力

**优势**：
- 连接到任何 MCP 兼容数据源
- 基于配置的动态工具加载
- 向后兼容（MCP 不可用时回退到自定义工具）
- 支持中国股市数据源

**实现**: [tradereact/agents/utils/mcp_loader.py](tradereact/agents/utils/mcp_loader.py)

### 🗄️ 2. Weaviate 向量数据库

**功能**: 用 Weaviate 替换 ChromaDB 进行高级内存管理

**优势**：
- 混合搜索：BM25 关键词 + 向量相似度
- 更好的语义理解
- 可配置搜索参数（alpha、limit）
- 支持本地和云部署
- 生产级可扩展性

**实现**: [tradereact/agents/utils/memory.py](tradereact/agents/utils/memory.py)

### ⚡ 3. 优化路由

**功能**: 通过智能路由减少不必要的分析师调用

**优势**：
- 路由轮次减少 75%（平均从 4 轮降至 1 轮）
- 更快的决策
- 更低的 API 成本
- 保持决策质量

**实现**: [tradereact/graph/subgraphs/analyst_subgraph.py](tradereact/graph/subgraphs/analyst_subgraph.py)

### 🎯 4. 配置驱动架构

**功能**: 集中式配置管理

**优势**：
- 轻松定制部署
- 无需修改代码即可配置
- 更适合开源协作
- 环境特定配置

**文件**：
- [tradereact/default_config.py](tradereact/default_config.py)
- [mcp_config.json](mcp_config.json)

### 🌍 5. 多市场支持

**功能**: 增强对非美国市场的支持

**优势**：
- 通过 MCP 集成中国股市
- 可扩展至其他市场
- 本地化数据源
- 区域分析能力

---

## 项目结构

```
TradeReAct/
├── tradereact/
│   ├── agents/
│   │   ├── analysts/          # 分析师智能体（4 种类型）
│   │   ├── researcher/        # 看多/看空研究员
│   │   ├── trader/           # 交易决策智能体
│   │   ├── risk_manager/     # 风险管理团队
│   │   └── utils/
│   │       ├── memory.py      # Weaviate 内存系统
│   │       └── mcp_loader.py  # MCP 工具加载器
│   ├── graph/
│   │   ├── trading_graph.py   # 主协调器
│   │   └── subgraphs/        # 智能体子图
│   ├── dataflows/             # 数据获取工具
│   └── default_config.py      # 默认配置
├── mcp_config.json            # MCP 服务器配置
├── cli/                       # 命令行界面
└── requirements.txt           # Python 依赖
```

---

## 系统要求

### 最低配置
- 8GB 内存
- 4 核 CPU
- 10GB 磁盘空间

### 推荐配置
- 16GB+ 内存
- 8+ 核 CPU
- SSD 存储
- GPU（可选，用于本地 LLM 推理）

---

## 测试

### 健康检查

运行全面的项目验证：

```bash
python check_project.py
```

验证内容：
- 项目结构
- 模块导入
- MCP 配置
- Python 语法
- 环境变量

### 完整图测试

```bash
python test_full_graph.py
```

测试包含所有智能体和 MCP 集成的完整图构建。

---

## 性能优化

### 成本优化
- 使用 `gpt-4o-mini` 进行快速思考（比 GPT-4 便宜 10 倍）
- 使用 `o4-mini` 而非 `o1-preview` 进行深度思考
- 减少 `max_debate_rounds` 以加快决策
- 利用 MCP 工具减少自定义 API 调用

### 速度优化
- 本地测试使用内嵌 Weaviate
- 仅为必需分析师启用 MCP
- 调整内存检索限制
- 对重复查询使用缓存

---

## 故障排除

### MCP 工具未加载

1. 检查 `mcp_config.json` 格式
2. 验证服务器 URL 可访问
3. 测试：`python -m tradereact.agents.utils.mcp_loader`
4. 检查服务器 `enabled` 状态

### Weaviate 连接问题

1. 验证 Weaviate 正在运行（如使用本地实例）
2. 检查端口配置
3. 尝试内嵌模式：`"use_embedded": True`
4. 检查云实例的网络连接

### 内存/性能问题

1. 减少配置中的内存检索限制
2. 使用更轻量的 LLM 模型
3. 减少辩论轮次
4. 清除 Weaviate 缓存/集合

---

## 贡献

我们欢迎贡献！感兴趣的领域：
- 额外的 MCP 服务器集成
- 新的数据供应商适配器
- 性能优化
- 文档改进
- Bug 修复

---

## 引用

本项目基于原始 TradingAgents 框架：

```bibtex
@misc{xiao2025tradingagentsmultiagentsllmfinancial,
      title={TradingAgents: Multi-Agents LLM Financial Trading Framework},
      author={Yijia Xiao and Edward Sun and Di Luo and Wei Wang},
      year={2025},
      eprint={2412.20138},
      archivePrefix={arXiv},
      primaryClass={q-fin.TR},
      url={https://arxiv.org/abs/2412.20138},
}
```

---

## 许可证

本项目遵循与原始 TradingAgents 仓库相同的许可证。

---

## 免责声明

本框架仅用于研究和教育目的。不构成财务、投资或交易建议。交易涉及重大风险，过去的表现不保证未来结果。在做出投资决策之前，请始终进行自己的研究并咨询合格的财务顾问。

---

## 致谢

- 原始 [TradingAgents](https://github.com/TauricResearch/TradingAgents) 由 Tauric Research 开发
- [LangChain](https://github.com/langchain-ai/langchain) 和 [LangGraph](https://github.com/langchain-ai/langgraph) 框架
- [Weaviate](https://weaviate.io/) 向量数据库
- Anthropic 的 [模型上下文协议](https://modelcontextprotocol.io/)
- Alpha Vantage 的 API 支持
